# 两可性

每个动词都有两种定义：当它同时有左参数和右参数时使用双子（**dyad**），当它只有右参数时使用单子（**monad**）。
这两个定义通常是相关的，比如`-`的双子定义表示减法，而单子定义表示求负。
```j
   5 - 3
2
   - 7
_7
```

双子也被称为动词的二元模式，而单子被称为动词的一元模式。
“两可性”一词用于化学意义上的两种价态，表明一个动词既可以与一个参数反应，也可以与两个参数反应。

**记住**：绝大多数动词都有单子和双子两种定义。