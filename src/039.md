# =.与=:何时类似

你已经看到了`=.`和`=:`用于动词定义时的不同之处。

当你在`Term`窗口中执行句子时，你不会在动词内执行它们，此时`=.`和`=:`有相同的效果。
```j
   NB. In the Term window

   a =. 123
   a
123
   a =: 234
   a
234
```

在`Term`窗口中连词`=.`和`=:`是相同的，你使用哪个来定义名字并不重要，因为它们都定义了全局名字。
`=.`更容易输入，也更容易被使用。但严格来说，在定义全局名字时，最好显式地使用`=:`。