[Header 1 ("第三个检查点", [], []) [Str "第三个检查点"], Para [Str "到了这个检查点，你应该明白："], BulletList [[Plain [Str "动词调试的一般方法"]], [Plain [Str "控制词如何将将句子分组成块来创建控制结构"]], [Plain [Str "T块测试结果是什么"]], [Plain [Str "测试结果如何决定执行哪个B块"]], [Plain [Str "测试结果如何决定控制结构的执行何时结束"]]], Para [Str "通过做以下练习来检查你的理解："], BulletList [[Plain [Str "单步调试温度转换动词"]], [Plain [Str "创建一个临时脚本文件，并定义一个名为", Code ("", [], []) "conv", Str "的动词，它类似于温度转换，但坚持使用", Code ("", [], []) "'f'", Str "参数来进行华氏度的转换，并且当", SoftBreak, Str "做参数既不是", Code ("", [], []) "'f'", Str "也不是", Code ("", [], []) "'c'", Str "时，返回一个字符串表示错误。", SoftBreak, Strong [Str "提示"], Str "：使用下面的控制结构示例，或者使用", Code ("", [], []) "select.", Str "控制结构"], CodeBlock ("", ["j"], []) "if. x = 'c' do. ...

  elseif. x = 'f' do. ...

  elseif. 1 do. 'left arg not c or f'

end.

"], [Plain [Str "创建一个临时脚本文件，并定义一个名为", Code ("", [], []) "plus", Str "的双子，将其左参数和右参数相加。但是，如果有错误，它应该给出一个字符串结果。", SoftBreak, Strong [Str "提示"], Str "："], BulletList [[Plain [Str "使用", Code ("", [], []) "dyad : 0", Str "；"]], [Plain [Code ("", [], []) "4 plus 9", Str "应当返回13；"]], [Plain [Code ("", [], []) "'a' plus 9", Str "应当返回错误字符串（比如，", Code ("", [], []) "'there was an error'", Str "）;"]], [Plain [Str "使用", Code ("", [], []) "try.", Str "控制结构来捕捉错误并给出字符串结果。"]]]]]]