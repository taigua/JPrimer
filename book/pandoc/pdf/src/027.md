[Header 1 ("字符串", [], []) [Str "字符串"], Para [Str "用单引号括起来的字符创建一个字符串。"], CodeBlock ("", ["j"], []) "   char =. 'Q'

   chars =. 'this is a list of characters' 

", Para [Str "显示字符串时不显示单引号。"], CodeBlock ("", ["j"], []) "   char

Q

   chars

this is a list of characters

", Para [Str "单引号用于字符串的开始和结束。一对单引号表示引号字符本身在字符串中。"], CodeBlock ("", ["j"], []) "   'put 2 ''s to get 1 '' in the string'

put 2 's to get 1 ' in the string

", Para [Str "不匹配的单引号是错误的。"], CodeBlock ("", ["j"], []) "   abc =. 'asdf

|open quote

", Para [Str "字符串可以为空。"], CodeBlock ("", ["j"], []) "   abc =. ''

", Para [Str "字符串也被称为字面值常量或字符常量。"]]