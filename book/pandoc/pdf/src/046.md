[Header 1 ("调试处理错误", [], []) [Str "调试——处理错误"], Para [Str "让我们在你的摄氏度动词中引入一个错误，看看它是什么样子的，以及你如何发现和修复它。"], Para [Str "打开你的", Code ("", [], []) "cf.ijs", Str "脚本并编辑第一行，通过在连词", Code ("", [], []) "=.", Str "右侧的表达式两边添加引号来制造错误。"], CodeBlock ("", ["j"], []) "t1 =. 'y - 32'

", Para [Code ("", [], []) "t1", Str "不再被定义为", Code ("", [], []) "y - 32", Str "的结果，它将被定义为字符串。"], Para [Str "运行脚本生成新的定义。关闭调试暂停，并清除断点，然后运行有bug的摄氏度动词。"], CodeBlock ("", [""], []) "   dbr 0	NB. disable suspension

   dbss ''

   centigrade 212 NB. Error messages in versions prior to J9.4

|domain error

|   t2=.t1    *5


   centigrade 212    NB. Expanded error messages in J9.4

|domain error in centigrade, executing dyad *

|x is character

|   t2=.t1    *5

", Para [Str "你正在禁用暂停（", Code ("", [], []) "dbr 0", Str "）的情况下运行，所以执行没有在摄氏度上暂停，缩进依然是正常的3个空格。"], Para [Str "如果你看一下错误行，很明显，", Code ("", [], []) "5", Str "是乘法的有效参数，所以", Code ("", [], []) "t1", Str "一定有问题。但是你不知道", Code ("", [], []) "t1", Str "的值。", SoftBreak, Str "你可以查看源代码找错误，但是，在复杂的情况下，使用debug可能会更快。"], Para [Str "启用暂停并重新运行。"], CodeBlock ("", ["j"], []) "   dbr 1	NB. enable suspension

   centigrade 212

|domain error

|   t2=.t1    *5

|centigrade[1]

", Para [Str "此时光标会缩进6个空格表示暂停，因为摄氏度被暂停了，你可以看看", Code ("", [], []) "t1", Str "的值。"], CodeBlock ("", ["j"], []) "      t1

y - 32

", Para [Str "从", Code ("", [], []) "t1", Str "的显示可以清楚地看出它是一个字符串，而不是所需计算的数字。现在可以查看源代码，", SoftBreak, Str "看看", Code ("", [], []) "t1", Str "是在哪里定义的，并发现引号不应该出现在那里。"], Para [Str "编辑源代码，通过删除引号来修复定义，并运行脚本来重新定义摄氏度动词。"], Para [Str "要重新运行第0行来正确地定义", Code ("", [], []) "t1", Str "，你可以通过使用", Code ("", [], []) "dbjmp", Str "从第0行继续执行。"], CodeBlock ("", ["j"], []) "      dbjmp 0

100

", Para [Str "由于没有设置断点，也没有其他错误，因此摄氏度第0行被执行，它将给局部名字", Code ("", [], []) "t1", Str "设置正确的值，并继续执行直到完成。"]]