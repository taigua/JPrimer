[Header 1 ("全局名字", [], []) [Str "全局名字"], Para [Str "在动词执行之外定义的名字是全局名子。"], Para [Str "上一节中，", Code ("", [], []) "Term", Str "窗口中定义的", Code ("", [], []) "t1", Str "是一个全局名子，与动词摄氏度内定义的", Code ("", [], []) "t1", Str "完全不同。"], Para [Str "让我们来做些实验。使用", Code ("", [], []) "File | New temp", Str "创建一个临时脚本文件，并在其中输入以下定义："], CodeBlock ("", ["j"], []) "fooa =: 3 : 0   NB. =: is important

zzz + y

)

", Para [Str "使用", Code ("", [], []) "Run | Load script", Str "运行脚本。Term窗口显示："], CodeBlock ("", ["j"], []) "   fooa 5

|value error: zzz

|       zzz+y

", Para [Str "让我们定义全局", Code ("", [], []) "zzz", Str "，看看会发生什么。在动词外定义它，无论我们使用", Code ("", [], []) "=.", Str "还是", Code ("", [], []) "=:", Str "，它都是全局的。"], Para [Str "为了遵循最佳实践，我们将在", Code ("", [], []) "Term", Str "窗口中使用", Code ("", [], []) "=:", Str "："], CodeBlock ("", ["j"], []) "   zzz =: 23	NB. define global zzz

   fooa 5

28

", Para [Str "动词", Code ("", [], []) "fooa", Str "使用全局的", Code ("", [], []) "zzz", Str "。动词可以使用全局名字。"], Para [Str "编辑脚本以添加", Str "```", Str "foob", Str "`", Str "，然后运行脚本。"], CodeBlock ("", ["j"], []) "foob =: 3 : 0

zzz =. 7

zzz + y

)

", Para [Str "在", Code ("", [], []) "Term", Str "窗口中："], CodeBlock ("", ["j"], []) "   foob 3

10

   zzz

23

", Para [Str "动词", Code ("", [], []) "foob", Str "使用它的局部", Code ("", [], []) "zzz", Str "而忽略全局", Code ("", [], []) "zzz", Str "。因此，动词可以使用局部名字而忽略同名的全局名字。"], Para [Str "如果你想定义一个全局名字呢？全局连词", Code ("", [], []) "=:", Str "（带", Code ("", [], []) ":", Str "词形变化的", Code ("", [], []) "=", Str "）定义了一个全局名字。编辑脚本以添加", Code ("", [], []) "fooc", Str "，然后运行脚本。"], CodeBlock ("", ["j"], []) "fooc =: 3 : 0

gw =: y

lz =. y

)

", Para [Str "在", Code ("", [], []) "Term", Str "窗口中："], CodeBlock ("", ["j"], []) "   fooc 3

3

   gw

3

   lz

|value error: lz

   gw =: 24  NB. Using =: to assign a global value, although assignment with =.  outside of a verb would also be global.

   fooc 5

5

   gw

5

", Para [Str "用", Code ("", [], []) "=:", Str "定义", Code ("", [], []) "gw", Str "，则定义了全局名字。"], Para [Str "一般来说，在动词中只定义局部名字而不定义全局名字是一种很好的做法。这是有时被称为函数式编程风格的重要组成部分。", SoftBreak, Str "定义全局名字的动词被认为具有副作用，并且更有可能导致bug，并且使阅读应用程序以理解正在发生的事情变得更加困难。", SoftBreak, Str "事实上，在J的最新版本中，当你定义一个同时使用同名全局名字和局部名字定义的动词并且运行它，将会触发一个域错误。"]]